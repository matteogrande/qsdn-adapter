# Override any Settings field here (loaded by pydantic-settings).
# Defaults already target the docker-compose network, so this file is
# only needed for local (non-docker) runs.
SERVICE_NAME=local
IDENTITY_URL=http://localhost:8081
TOPOLOGY_URL=http://localhost:8082
PCE_URL=http://localhost:8083
KOM_URL=http://localhost:8084
PROVISIONING_URL=http://localhost:8085
AUDIT_URL=http://localhost:8086
TOPOLOGY_CONFIG=./config/topology.yaml
KMS_REGISTRY=./config/kms_registry.yaml
AUDIT_DB_PATH=./data/audit.db
